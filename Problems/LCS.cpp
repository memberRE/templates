#include<iostream>
#include <cstdio>
#include <algorithm>
using namespace std;
const int MAX = 1e5 + 20;
int n, pic[MAX];
int main()
{
    while(scanf("%d",&pic[++n])!=EOF)
        ;
    n--;
    cout << n;
}