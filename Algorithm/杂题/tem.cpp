#include <algorithm>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <stack>
#include <vector>
#define ls (node << 1)
#define rs ((node << 1) | 1)
typedef long long ll;
using namespace std;
int main()
{
    ll j1 = (((ll)(1 << 31) - 1ll) * ((ll)(1 << 31) - 1ll) / 12) * 13;
    ll j2 = ((((ll)(1 << 31) - 1ll) / 2) << 31) + (((ll)(1 << 31) - 1ll) / 2);
}